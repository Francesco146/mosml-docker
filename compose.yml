services:
    mosml:
        image: mosml:latest
        build:
            context: .
            dockerfile: Dockerfile
            platform: linux/amd64
        container_name: mosml
        volumes:
            - ./src:/workspace
        stdin_open: true
        tty: true
